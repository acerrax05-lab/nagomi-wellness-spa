{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Backend",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev"],
      "cwd": "${workspaceFolder}/spa-backend",  // ← ADD THIS
      "skipFiles": ["<node_internals>/**"],
      "console": "integratedTerminal"           // ← ADD THIS so Python logs are visible
    }
  ]
}